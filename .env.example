# Public API base for frontend (optional when using domain route)
# Example: https://your-backend.up.railway.app/api
SUPPORT_API_BASE=

# Backend database (Railway MySQL)
DATABASE_URL=mysql://user:password@host:port/database
DB_SSL=true
DB_ALLOW_MEMORY_FALLBACK=false

# App runtime
NODE_ENV=production
PORT=3000
